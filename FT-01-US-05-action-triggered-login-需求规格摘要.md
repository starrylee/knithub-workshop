# FT-01-US-05 操作触发登录 · 需求规格摘要

> 来源需求文档：`FT-01-US-05-action-triggered-login.md`（Feature: FT-01 账号与鉴权）
> 澄清方式：需求澄清访谈（逐条确认）；**需求变更口径 A（2026-09-08 已由需求方确认）**见文末变更记录。

---

## 一、背景摘要

未登录访客想做需登录的操作时，应被引导到登录弹窗，在弹窗内完成登录后**自动续接刚才想做的事**，从而顺畅完成操作、不被"请先登录"的墙挡住。

职责边界：FT-01-US-05 提供通用"操作触发登录"机制（弹窗 + 登录续接 + 会话上下文）；创建项目 / 打卡 / 点赞 / 留言等**具体受限操作的接入**由 FT-02 / FT-03 / FT-04 按各自口径实施，不属于本故事。

**变更后（口径 A）：受限操作触发入口 = 导航栏「图案库 / 我的项目 / 社区 / 关于」；全站仅「首页 `/`」为公开落地页。**（变更前为仅「我的项目」一个触发点、四个内容页公开免登录。）

## 二、已确认的决策清单

| # | 决策点 | 最终选择 |
|---|---|---|
| D1 | [角色] 使用主体 | 未登录的访客（文档原文，无分歧） |
| D2 | [行为] 受限入口接入范围 | **变更后（口径 A）**：导航栏「图案库 `/patterns` / 我的项目 / 社区 `/community` / 关于 `/about`」均为受限入口；仅「首页 `/`」保留为公开落地页 |
| D3 | [行为] 内容页公开属性 | **变更后（口径 A）**：原 AC-3「公开页免登录浏览」撤销——图案库/社区/关于不再对未登录访客公开（入口受登录拦截） |
| D4 | [行为] AC-2 登录通道 | 仅实例化登录路径（`woolenwhimsy / knit123`）；注册 tab 当前在站点不可用（提示"注册功能即将上线"），注册续接**超出本故事**，待注册能力上线后复用同一续接机制 |
| D5 | [行为] 触发来源 | 未登录访客可从唯一公开落地页（首页 `/`）触发任意受限入口（图案库/我的项目/社区/关于）；后续内容页均需登录后进入，不存在"从受限页再触发"场景 |
| D6 | [行为] 登录失败路径 | 纳入"失败不续接"断言——错误凭证时弹窗不关闭、停留当前页、显示错误提示、不进入受限目标页 |
| D7 | [目标] 达成效果 | 访客顺畅完成受限操作，不被登录墙挡住（文档原文，无分歧） |

## 三、最终验收标准列表（口径 A）

### E1（对应 AC-1）受限导航入口触发登录弹窗
- Given：访客处于未登录状态，正在浏览首页 `/`
- When：分别点击导航栏「图案库」「社区」「关于」「我的项目」
- Then：每次均**弹出登录弹窗**（标题"欢迎回来"），且 **URL 保持首页 `/` 不变**（停留在当前页，不发生页面拦截跳转）

### E2-1（对应 AC-2）登录成功自动续接到目标页
- Given：未登录访客浏览首页，点击某受限入口后弹出登录弹窗
- When：以 `woolenwhimsy / knit123` 登录成功
- Then：登录弹窗关闭，页面**自动续接进入该入口的目标页**，无需再次点击：
  - 图案库 → `/patterns`
  - 社区 → `/community`
  - 关于 → `/about`
  - 我的项目 → `/users/woolenwhimsy`

### E2-2（对应 AC-2 失败路径）登录失败不续接
- Given：未登录访客点击「图案库」弹出登录弹窗
- When：输入错误凭证（如 `woolenwhimsy` / 错误密码）
- Then：登录弹窗**不关闭**、页面停留在首页、显示"用户名或密码错误"提示、URL 未跳转为受限目标页

### E3（口径 A 重定义，原"公开免登录浏览"撤销）登录态直达
- Given：访客通过受限入口登录成功（如已进入 `/community`）
- When：再点击其它受限入口（如「图案库」）
- Then：页面**直接进入** `/patterns`（已登录用户免二次弹窗），全程无登录弹窗出现

### 范围外（明确不验收）
- 注册通道的注册成功/续接（注册能力当前不可用，属 FT-01-US-02 待办范畴）
- 未登录访客对受限内容页（`/patterns`、`/community`、`/about`）的公开免登录浏览（口径 A 已撤销该项，全站仅首页公开）
- 创建项目 / 打卡 / 点赞 / 留言等其他受限操作的触发接入（归 FT-02/03/04）
- 登录态跨刷新持久化/登出后端销毁会话（既有 TODO，非本故事）
- 未登录用户通过 URL 直接输入受限路径的访问控制（本故事仅约束导航入口触发；路由级守卫不在本故事范围）

## 四、验收标准三问自检结果

| 验收项 | 是否具体动作 | 是否可观察结果 | 非技术评审可否直接判断 |
|---|---|---|---|
| E1 | 是（逐入口点击、查看弹窗与 URL） | 是（弹窗出现、URL 不变） | 是 |
| E2-1 | 是（输入账号密码、观察自动续接） | 是（弹窗关闭、进入目标页） | 是 |
| E2-2 | 是（输入错误凭证、观察反馈） | 是（弹窗不关、报错文案、URL 不变） | 是 |
| E3 | 是（登录后再次点击受限入口） | 是（直达目标页、无弹窗） | 是 |

结论：全部通过，无需重新追问。

## 五、现状基础（已核实，供开发参考，非验收项）

- 站点路由 `/`、`/patterns`、`/community`、`/about`、`/users/:username` 均无路由级鉴权守卫；受限拦截由导航入口触发实现（`Navbar` 点击处理），URL 直录仍可到达（见"范围外"最后一条）。
- `Navbar`「图案库 / 我的项目 / 社区 / 关于」均为 `authRequired` 受限入口：未登录点击 → `preventDefault` + 记录续接目标 + 弹出 `LoginModal`（不跳转）；已登录点击 → 默认 Link 直达。
- `LoginModal` 支持 `redirectTo`：登录成功 → 关闭弹窗并 `navigate(redirectTo)`（续接到受限入口目标页）；未携带 `redirectTo` 时维持跳个人页；登录失败 → 弹窗内提示"用户名或密码错误"，不关闭不跳转；注册 tab 提交提示"注册功能即将上线"。
- 登录能力（FT-01-US-02/03）已可用：`POST /api/v1/auth/login` 成功建立会话并返回用户信息；演示账号 `woolenwhimsy / knit123`、`threadcountess / fiber456`。
- 文档依赖标注：US-05 依赖于 US-02（登录能力）、US-03（登录态与当前用户上下文）。

## 六、需求变更记录

- **2026-09-08（口径 A，已由需求方确认）**：原口径「图案库/社区/关于 = 公开免登录浏览页（AC-3/E3）」**撤销**，改为与「我的项目」一致的**受限入口**——未登录点击弹登录弹窗并自动续接到对应目标页；仅「首页 `/`」保留为公开落地页。受影响的决策：D2、D3、D5；受影响的验收：E1、E2-1、E2-2、E3。实现已同步（web-ui Navbar/LoginModal、e2e feature 与步骤、本规格文件）。

---
**需求已澄清并完成口径 A 变更，可以进入方案设计阶段。**
