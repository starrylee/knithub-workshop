# FT-02-US-01 创建编织项目

> 属于 Feature: FT-02 项目建档与看板管理

**状态**：评估通过
**优先级**：P0
**估算**：待估算
**拆分模式**：Workflow Steps

---

## 用户故事

```
作为一个 织女用户（已登录），
我想要 创建一个新的编织项目（填写名称与预计完成日期，可选填里程碑），
以便 为每段编织旅程建立档案，后续在看板上追踪它的状态。
```

## 验收标准（AC）

**AC-1: 成功创建项目**
- **Given** 我已登录，处于"我的项目 → 项目"Tab
- **When** 我点击"新建项目"，填写项目名称与预计完成日期后提交
- **Then** 项目创建成功，初始状态为"待开始"，并出现在"项目"Tab 的项目列表中

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | 用户 Iris 已登录，项目 Tab 列表为空 | 点击"新建项目"，名称填"祖母的生日披肩"，预计完成日期选 2026-10-31，不填里程碑，提交 | 创建成功，项目列表出现"祖母的生日披肩"，状态"待开始" |

**AC-2: 创建时可选携带里程碑**
- **Given** 我正在填写新建项目表单
- **When** 我在基本信息之外添加 1 个或多个里程碑（名称必填、预计日期可选）后提交
- **Then** 项目与里程碑一并创建成功，里程碑按填写顺序排列且均为未完成状态

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | Iris 打开新建项目表单 | 名称"圣诞袜"、日期 2026-12-24，添加里程碑"织袜筒"（预计日期 2026-12-01）与"收口"（不填日期），提交 | 项目"圣诞袜"创建成功，里程碑列表依次显示"织袜筒"、"收口"，均未完成 |

**AC-3: 必填项校验**
- **Given** 我正在填写新建项目表单
- **When** 项目名称为空或未选择预计完成日期就提交
- **Then** 表单提示对应必填项缺失，项目不被创建

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | Iris 打开表单，名称留空，日期已选 2026-11-15 | 点击提交 | 名称处提示"请填写项目名称"，未创建项目 |
| 示例 2（反例） | Iris 打开表单，名称填"围巾"，日期未选 | 点击提交 | 日期处提示"请选择预计完成日期"，未创建项目 |

**AC-4: 表单内里程碑名称必填**
- **Given** 我在新建项目表单中添加了里程碑
- **When** 某条里程碑名称为空就提交
- **Then** 提示里程碑名称必填，项目不被创建

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | Iris 表单中项目名称、日期已填，添加 1 条里程碑但名称留空 | 点击提交 | 提示"里程碑名称必填"，项目未创建 |

**AC-5: 未登录不可创建**
- **Given** 我未登录
- **When** 我尝试进入项目创建入口
- **Then** 被引导至登录，无法创建项目

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | 游客未登录，直接访问项目 Tab 并点击"新建项目" | 点击创建入口 | 跳转登录页，项目列表无任何新增 |

## 依赖关系

- 依赖于: 无
- 被依赖: FT-02-US-02、FT-02-US-03、FT-02-US-04、FT-02-US-05、FT-02-US-06

## 备注

- 技术要点（来自 AskMe 已确认约束）：项目实体采用严格最小字段集（名称 / 预计完成日期 / 状态 / 里程碑 / `is_public` 预留 + 内部 id / 创建时间 / 最近活动时间）；`is_public` 仅结构预留，读写逻辑归 FT-04（裁决 1）；"最近活动时间"建档时初始化为创建时间，FT-03 打卡接入后更新为 max(最近打卡时间, 创建时间)
- 后端为新增项目创建 API，复用 FT-01 Java SpringBoot 基建，JSON 文件持久化须经 Repository 抽象层（PostgreSQL 兼容设计）
- 本卡片实例化示例中的具体场景数据（人名 / 项目名 / 日期）为 AI 草拟，业务规则（必填项、初始状态"待开始"、里程碑可选）来自 AskMe 已确认决策，建议开发前与 PO 核实具体数据
- 项目名称允许同名（个人工具场景，无唯一性约束）；名称长度上限未在 AskMe 中定义，由实现按常规表单约束处理

---

**创建日期**：2026-09-05
**维护者**：AI Agent (asdm-feature-split)
