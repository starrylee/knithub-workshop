# 需求规格摘要：FT-01-US-03 登录态保持与当前用户上下文

- 澄清日期：2026-09-08
- 澄清方式：需求访谈（PO 模式）
- 依据文档：`FT-01-US-03-session-persistence-and-user-context.md`
- 状态：✅ 需求已澄清完毕，可以进入方案设计阶段

---

## 一、背景摘要

KnitHub 登录功能（FT-01-US-02）已完成：前端 `AuthContext.login` 调用 `POST /api/v1/auth/login`，服务端以内存 Session（`SessionManager`）+ HttpOnly Cookie `sid` 维护会话。当前缺口在于：`AuthContext` 初始化时 `currentUser` 硬编码为 `null`，**没有会话恢复逻辑**——用户刷新页面或重开浏览器后，服务端明明还有会话，前端却已变成游客态。

本 Story 目标是补齐会话恢复，让登录态在刷新/重开浏览器后保持，并向下游模块（FT-02/03/04）稳定供给"当前用户是谁"的上下文。

## 二、已确认的决策清单

| # | 决策点 | 澄清结果 |
|---|---|---|
| 1 | 服务对象 | 终端用户（织女，免重复登录）＋系统内消费者（FT-02/03/04 经 AuthContext 读取身份）＋游客态（须能识别为"空"） |
| 2 | 游客态覆盖范围 | **扩展**：除"已登出"外，**从未登录过的新访客**读取当前用户也必须为空，禁止出现"半空半有"脏状态 |
| 3 | 会话恢复期间的 UI 表现 | **B 方案（初始化中状态）**：恢复完成前界面不显示登录/游客信息（用户名区留空占位），恢复完成后再渲染真实状态；不允许出现"先闪游客态再跳回登录态" |
| 4 | 目标表述 | 登录成功后，刷新页面 / 重开浏览器（本机、同一浏览器）均直接认出用户、无需再登录；站点内所有功能模块可稳定问到"当前用户是谁"，直到**主动登出** |
| 5 | 边界 1：服务重启 | 全体登录态丢失、需重新登录——已接受的 MVP 取舍，不作为缺陷处理 |
| 6 | 边界 2：无显式过期 | 只要服务不重启，登录态永久有效，不引入过期机制 |
| 7 | 边界 3：登出即失效 | 用户登出后会话立即销毁，恢复为空游客态 |
| 8 | 示例数据性质 | AC 实例化示例中的时间点（如"周五 21:00/周六 09:00"）为演示性数据，不承担验收语义，仅表达"跨时段 + 服务未重启"；测试无需对齐真实日历 |

## 三、最终验收标准列表

| # | 验收条目 | 关键动作 → 可观察结果 |
|---|---|---|
| AC-1 | 刷新页面保持登录 | Given 用户已登录；When 连续刷新页面（F5）3 次；Then 每次加载完成 Navbar 显示用户名，全程未出现登录弹窗 |
| AC-2 | 重开浏览器保持登录 | Given 用户已登录后完全关闭浏览器（服务未重启）；When 重新打开浏览器访问 KnitHub；Then 直接处于登录态，Navbar 显示用户名，无需重新输入用户名密码 |
| AC-3a | 下游读取到身份 | Given 用户处于登录态；When 任意模块（含 FT-02/03/04）经 AuthContext 读取当前用户；Then 得到 username、displayName、avatar |
| AC-3b | 游客态读取为空（含新访客） | Given 用户已登出 **或从未登录过的新访客**；When 同一位置读取当前用户；Then 结果为 null，下游识别为游客态 |
| AC-4 | 恢复期不闪游客态 | Given 用户已登录；When 刷新页面；Then 用户名出现前导航栏不得闪现"登录/注册"等游客控件，无掉线又恢复的视觉闪烁 |
| AC-5 | 边界负向验证 | When 用户登出后刷新页面 / 后端服务重启后刷新页面；Then 均呈现游客态，登录态不自动恢复 |

> 自检说明：以上 6 条均为"具体动作 + 可观察结果 + 非技术人员可肉眼判断"，全部通过验收三问自检。

## 四、明确不做的事（本次范围外）

- ❌ 不做显式会话过期/续期机制（见边界 2）
- ❌ 不处理服务重启后的会话恢复（见边界 1，属已接受取舍）
- ❌ 不实现记住密码/多设备登录等超出本 Story 的能力
- ❌ 不改变登录（US-02）与登出（US-04）的既有接口行为
