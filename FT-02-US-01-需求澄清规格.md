# FT-02-US-01 创建编织项目 · 需求澄清规格

> 基于 `FT-02-US-01-project-creation.md` 的访谈澄清结果
> 澄清方式：需求澄清访谈（一次一问，四要素 + 验收三问自检）
> 生成日期：2026-09-08

---

## 一、背景摘要

FT-02（项目建档与看板管理）的首个用户故事：已登录的织女用户需要一种**快速**的方式，把脑中的编织计划变成一条可追踪的项目档案。本期（P0）只解决"能不能建档、建档后能不能在列表看到"这一件事；看板状态流转、详情页、公开分享等交给 FT-02 后续 US 与 FT-04。

原始卡片已有 5 条 AC，但缺少：使用人群边界、表单载体与提交落点、里程碑数量约束、字段长度/日期范围、后端 API 契约。本规格补齐这些决策，使开发者可直接开工。

---

## 二、已确认决策清单

| # | 要素 | 决策 | 说明 |
|---|---|---|---|
| D1 | 角色 | **仅限已登录用户为自己建档** | 项目 `ownerId` = 当前登录用户；无代建/游客草稿；未登录一律引导登录（与 AC-5 一致）。`is_public` 仅结构预留，公开读写归 FT-04。 |
| D2 | 行为（表单与落点） | **弹窗（Modal）创建，提交后回到「我的项目 → 项目」Tab 列表** | 与 FT-01 登录弹窗交互一致；本期无详情页/看板可跳，不引入范围外页面。 |
| D3 | 目标 | **主目标 = 快速建档（存得进、列得出）** | 建档即看板数据骨架的自然副产物，不前置"追踪/活跃"设计。 |
| D4 | 里程碑数量 | **不设条数上限；弹窗内里程碑区可滚动、可逐条删除** | 个人工具场景极少超量；尊重卡片"1 个或多个"。 |
| D5 | 字段约束 | **名称 1–100 字符（超长提示"名称不超过100字"）；预计完成日期任意可选，不限制过去/未来** | 与卡片备注"按常规表单约束处理"一致，保持 P0 范围最小。 |
| D6 | 列表排序与反馈 | **按创建时间倒序，新项目固定置顶（列表首位）；提交成功后顶部一次性 toast「项目已创建」约 2 秒自动消失** | 最新档案永远在顶部，验收最直观。 |
| D7 | 后端 API | **`POST /api/v1/projects`（需登录，走 FT-01 的 `sid` Cookie）** | 成功 `201` + 项目 JSON（含 `id`、状态 `"待开始"`、里程碑数组）；校验失败 `400` + 字段级错误；未登录 `401`（引导登录，与 FT-01 一致）。 |

---

## 三、最终验收标准列表

**AC-1 成功创建项目**
- Given 已登录用户处于「我的项目 → 项目」Tab
- When 点击「新建项目」→ 弹窗中填写**项目名称**与**预计完成日期**后提交
- Then 弹窗关闭、回到项目 Tab；新项目出现在**列表首位**，初始状态为「待开始」；顶部显示一次「项目已创建」提示（约 2 秒消失）
- 实例化：Iris 已登录、列表为空 → 新建「祖母的生日披肩」、日期 2026-10-31、不填里程碑 → 列表首位出现该项目，状态「待开始」

**AC-2 可选携带里程碑（无上限、可增删、按序、未完）**
- Given 正在填写新建项目表单
- When 在基本信息外添加 0 条或多条里程碑（名称必填、预计日期可选），里程碑区可滚动、可逐条删除
- Then 提交后项目与里程碑一并创建成功；里程碑按填写顺序展示，且均为未完成状态
- 实例化：Iris 新建「圣诞袜」、日期 2026-12-24，加里程碑「织袜筒」(2026-12-01) 与「收口」(无日期) → 项目创建成功，里程碑依次显示且均未完成

**AC-3 必填项校验**
- Given 正在填写新建项目表单
- When 项目名称为空 **或** 未选择预计完成日期就提交
- Then 对应字段下方提示（名称空→「请填写项目名称」；日期未选→「请选择预计完成日期」），项目不被创建
- 反例：名称空/日期已选 → 名称提示；名称已填/日期空 → 日期提示

**AC-4 表单内里程碑名称必填**
- Given 表单中已添加里程碑
- When 某条里程碑名称为空就提交
- Then 提示「里程碑名称必填」，项目不被创建
- 实例化：项目名+日期已填、1 条里程碑名称留空 → 提交提示、项目未创建

**AC-5 未登录不可创建**
- Given 游客未登录
- When 尝试进入项目创建入口（或直接调用创建 API）
- Then 被引导至登录（跳转登录页/弹登录窗）；无新增项目；API 返回 `401`
- 实例化：游客点「新建项目」→ 跳登录页，项目列表无新增

**AC-6 字段长度与状态码（派生，支撑可断言验收）**
- Given 已登录并打开新建项目表单 / 调用 API
- When 项目名称超过 100 字符 **或** 观察后端响应
- Then 前端提示「名称不超过100字」；成功时 API 返回 `201` + 项目 JSON（含 `id`、状态 `"待开始"`、里程碑数组）；校验失败返回 `400` + 字段级错误；未登录返回 `401`

---

## 四、范围外（本期不做，归入后续 US）

- 项目详情页 / 看板状态流转（FT-02-US-02~06）
- 公开分享 / `is_public` 读写（FT-04）
- 代他人建档、游客草稿（D1 已排除）
- 打卡与"持续使用"钩子（FT-03）

---

**需求已澄清完毕，可以进入方案设计阶段。**
