# FT-01-US-05 操作触发登录

> 属于 Feature: FT-01 账号与鉴权

**状态**：评估通过
**优先级**：P1
**估算**：待估算
**拆分模式**：Workflow Steps（账号生命周期入口场景：访客→登录的引导路径）

---

## 用户故事

```
作为一个未登录的访客，
我在想做需要登录的操作时想要被引导到登录弹窗、并在登录后继续刚才想做的事，
以便顺畅完成操作而不被一堵"请先登录"的墙挡住。
```

## 验收标准（AC）

**AC-1: 个人数据入口触发登录弹窗**
- **Given** 访客处于未登录状态
- **When** 点击个人数据入口（如 Navbar"我的项目"）
- **Then** 弹出登录弹窗（复用现有 Navbar authRequired 交互模式），不发生页面拦截跳转

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | 访客未登录，正在浏览首页 | 点击 Navbar"我的项目" | 弹出登录弹窗（停留在当前页面，非跳转拦截） |

**AC-2: 登录后自动续接原操作**
- **Given** 访客因点击受限操作弹出登录弹窗
- **When** 在弹窗内完成登录（或注册）
- **Then** 弹窗关闭，自动进入/继续原本想执行的操作（如进入"我的项目"）

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | 访客点击"我的项目"后弹出登录弹窗 | 以 woolenwhimsy / knit123 登录成功 | 弹窗关闭并自动进入"我的项目"页面，无需再手动点击一次 |

**AC-3: 公开内容浏览免登录**
- **Given** 访客处于未登录状态
- **When** 浏览公开内容（首页/广场/图案库/关于）
- **Then** 全程无登录拦截（不弹登录窗、不强制跳转登录页）

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | 访客未登录，从首页进入广场再进入图案库、关于页 | 连续浏览 4 个公开页面 | 全程无登录弹窗出现，可自由浏览内容 |

## 依赖关系

- 依赖于: FT-01-US-02（登录能力）、FT-01-US-03（登录态与当前用户上下文，续接操作时识别身份）
- 被依赖: FT-02 / FT-03 / FT-04（跨 Feature：按口径接入各自的需登录操作清单）

## 备注

- FT-01 职责边界：只提供通用"操作触发登录"机制（弹窗 + 登录续接 + 会话上下文）；创建项目/打卡/点赞/留言等具体操作的接入由 FT-02/03/04 按 AskMe 决策点 4 口径实施
- AC7 口径说明："浏览开放、互动登录"已与 AC7 原文兼容（登录用户可浏览仍成立），AC 文本无需修订（AskMe 决策点 4 已明确）
- 现有 Navbar 已验证的 authRequired 交互模式（"我的项目"未登录点击弹窗）为可直接复用的基础
- ⚠️ 实例化示例中的页面浏览序列为 AI 草拟（页面集合来自已确认的决策点 4 口径），建议开发前与 PO 核实

---

**创建日期**：2026-09-05
**维护者**：AI Agent (asdm-feature-split)
