# FT-01-US-03 登录态保持与当前用户上下文

> 属于 Feature: FT-01 账号与鉴权

**状态**：评估通过（全局决策点已确认，见 story-list.md 已确认决策点）
**优先级**：P1
**估算**：待估算
**拆分模式**：Workflow Steps（账号生命周期第 3 步：会话持续）

---

## 用户故事

```
作为一个已登录的织女，
我想要刷新页面或重开浏览器后仍保持登录状态，
以便不被打断地持续使用 KnitHub，也让我的项目、打卡和互动都知道"我是谁"。
```

## 验收标准（AC）

**AC-1: 刷新页面保持登录**
- **Given** 用户已登录
- **When** 刷新页面（浏览器刷新/F5）
- **Then** 页面加载完成后仍处于登录态（Navbar 显示用户名），无需重新登录

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | woolenwhimsy 于 09:00 登录成功 | 09:30 连续刷新页面 3 次 | 每次加载后 Navbar 均显示 woolenwhimsy，全程未出现登录弹窗 |

**AC-2: 重开浏览器保持登录**
- **Given** 用户已登录后关闭浏览器（服务未重启）
- **When** 重新打开浏览器访问 KnitHub
- **Then** 仍处于登录态（无需重新输入用户名密码）

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | woolenwhimsy 周五 21:00 登录后关闭浏览器，期间服务未重启 | 周六 09:00 重新打开浏览器访问站点 | 直接处于登录态，Navbar 显示 woolenwhimsy |

**AC-3: 向下游 Feature 供给当前用户上下文**
- **Given** 用户（如 woolenwhimsy）处于登录态
- **When** 前端任意功能模块（含 FT-02/03/04）经 AuthContext 读取当前用户
- **Then** 获取到该用户的身份信息（username、displayName、avatar）；未登录时读取结果为空（游客态）

**实例化示例**：

| 示例 | Given（具体数据） | When（具体操作） | Then（具体结果） |
| ---- | ------------------ | ------------------ | ------------------ |
| 示例 1 | woolenwhimsy 处于登录态 | FT-02 创建项目流程经 AuthContext 读取当前用户 | 得到 username=woolenwhimsy 及其 displayName、avatar（项目归属断言由 FT-02 侧验收） |
| 示例 2（反例） | 用户已登出 | 同一位置再次读取 AuthContext 当前用户 | 读取结果为空，下游识别为游客态 |

## 依赖关系

- 依赖于: FT-01-US-02（登录与会话建立）
- 被依赖: FT-02 / FT-03 / FT-04（跨 Feature：均消费当前用户上下文）

## 备注

- 会话为服务端内存 Session：服务重启会丢失登录态（全部用户需重新登录），为已确认 MVP 取舍，不作为缺陷处理
- 会话有效期口径已确认：无显式过期（服务不重启则一直有效，登出即失效），见 story-list.md 决策 2（2026-09-07）
- 实现参考（供估算，非实现规范）：页面加载/应用启动时的会话恢复查询（如 GET /api/me 类接口）+ AuthContext 初始化改造，将 mock 的 useState 会话替换为服务端会话状态
- 本 Story 合并了 FR-4（会话保持）与 FR-5（当前用户上下文供给）：两者实现高度重叠（同一会话查询与同一 AuthContext），拆开会导致后者不可脱离前者独立测试
- ⚠️ 实例化示例中的时间点（周五 21:00/周六 09:00 等）为 AI 草拟，建议开发前与 PO 核实

---

**创建日期**：2026-09-05
**维护者**：AI Agent (asdm-feature-split)
